<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
  
    <title>گران قیمت ترین ها</title>

    <style>
         @font-face {
            font-family: 'iransans';
            src: url('../font/Woff2/IRANSansXFaNum-Light.woff2') format('woff2'),
                url('../font/Woff/IRANSansXFaNum-Light.woff') format('woff');
            /* Add additional src lines for other font formats if necessary */
            font-weight: bold;
            font-style: normal;
        }

        body{
            font-family : iransans;
            background-image: url('/img/login.jpg');
            color: white;
        }


    </style>
</head>
<body dir="rtl">

<div class="container mt-3">

    <div class="row " style="display: flex; align-items: center; justify-content: center;">

        <h3 class="mb-5"> گران قیمت ترین کتاب های هر دسته بندی</h3>

    <table class="table" style="text-align: center">

        <tr style="align-items: center; color: white;">
            <th>ردیف</th>
            <th>نام دسته بندی</th>
            <th>کد کتاب</th>
            <th>عنوان کتاب</th>
            <th>قیمت کتاب</th>
            
        
        </tr>
            
        <% let counter = 1 ; %>
                <% books.forEach(book=>{ %>
                    
                        <tr style="align-content: start;">
                        
                            <td><%= counter%></td>
                            <% if(book.category == "null") {%>
                            <td>بدون دسته بندی</td>

                            <%}else {%>
                            <td><%= book.category %></td>
                            <%}%>
                            <td><%= book.ISBN %></td>
                            <td><%= book.title %></td>
                            <td><%= book.price %></td>
                           
                        </tr>
                
                            
                    
                    <% counter ++ ; %>
                <%})%>
            
            
    

</table>
</div>
<a href="/reports?username=<%= username %>" class="btn btn-danger">خروج</a>
</div>
    
</body>

</html>