<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        @font-face {
            font-family: 'iransans';
            src: url('../font/Woff2/IRANSansXFaNum-Light.woff2') format('woff2'),
                url('../font/Woff/IRANSansXFaNum-Light.woff') format('woff');
            /* Add additional src lines for other font formats if necessary */
            font-weight: bold;
            font-style: normal;
        }

        body {
            margin: 0;
            padding: 0;
            background-image: url('/img/login.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'iransans', sans-serif;
        }

        .container {
            background-color: rgba(0, 0, 0, 0.5); /* Dark background with opacity */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
            text-align: center;
            max-width: 400px;
        }

        .form-group {
            position: relative;
            margin-bottom: 20px;
        }

        .form-control {
            border: none;
            border-radius: 5px;
            padding-left: 35px; 
            background-color: #292b2c;
            color: #ffffff;
        }

        .form-control:focus {
            background-color: #343a40; 
            color: #ffffff; 
        }

        .form-control::placeholder {
            color: rgb(255, 255, 255); 
        }

        .input-icon {
            position: absolute;
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
            color: rgb(255, 255, 255);
        }

        .login-btn {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            background-color: #0b9b9d;
            color: #ffffff; 
            border: none;
            cursor: pointer;
        }

        .login-btn:hover {
            background-color: #11787a;
            color: #ffffff; 
        }

        .signup-link {
            color: #ffffff; 
            text-decoration: none;
            display: inline-block;
            margin-top: 10px; 
        }

        .signup-link:hover {
            text-decoration: none;
            color: rgb(204, 204, 204); 
        }
        .leave-btn {
            background-color: #dc3545; 
            color: #ffffff; 
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .leave-btn:hover {
            background-color: #ad2936; 
            color: #ffffff; 
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            color: white;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="text-center text-white mb-4">ورود کاربر</h2>
        <form action="/user-login" method="get" dir="rtl">
            <div class="form-group">
                <input type="text" class="form-control" name="userId" placeholder="نام کاربری">
                <i class="fas fa-user input-icon"></i> 
            </div>
            <div class="form-group">
                <input type="password" class="form-control" name="password" placeholder="رمز عبور خود را وارد کنید">
                <i class="fas fa-lock input-icon"></i> 
            </div>
            <button type="submit" class="btn login-btn">ورود</button>
        </form>
        <a href="/sign-in" class="signup-link">در صورت نداشتن حساب کاربری و ثبت نام کلیک کنید</a>
    </div>
    <button class="btn leave-btn"><i class="fas fa-sign-out-alt"></i> <a href="/start-page" style="text-decoration: none; color: white;">خروج</a></button>
    <!-- Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
